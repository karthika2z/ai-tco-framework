# Copy this file to .env and fill in your keys.
# The app works in Demo Mode without any keys (uses mock data).

# Required for Live Mode (real LLM generation)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the default data source
# LOOPQUANT_DATA_SOURCE=yfinance   # or "mock"
